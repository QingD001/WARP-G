# 理论升级路线(网络流思想清理版)

> 修订 2026-08-29:经讨论,原提案中的**闭包/min-cut 选择建模**与**联合分区-选择重构**判定为
> 不适合直接操作(硬 precedence 不是本课题的天然语言;本问题的瓶颈是收益预测的不确定性,
> 而非组合复杂度),已删除。保留三条可执行路线。

## 1. 子模性声明 + (1-1/e) 贪心保证(现在可做,零代码)

### 1.1 观察

对每条 query,$M(q; H, G_S)$ 对激活区域集 $S$ **很可能单调子模**:

- **单调**:多建一个区域的图只会向候选池加文档,检索结果不会变差;
- **子模**:候选池越满,新增图的边际增益越小。

若成立,目标是覆盖类函数的加权和,贪心有 **(1-1/e) 近似保证**(Nemhauser-Wolsey)。

### 1.2 落地方式(几乎零工程量)

1. **证明**:在"图检索结果经 RRF + CrossEncoder 融合、单调性来自候选池扩张"的假设下写出子模性论证;
2. **实验验证**:子模性等价于边际收益递减,用**已有的 probe pair interaction 数据**检验——
   交互项 $I(r_i, r_j) = U(\{i,j\}) - U(\{i\}) - U(\{j\}) + U(\varnothing) \le 0$ 的普遍成立
   与子模性直接对应。若交互分布普遍为负(边际递减),声明成立;若正负混杂,需限定条件措辞。

### 1.3 收益

设计文档中"不声称最优、只声称可部署近似"的免责声明可直接升级为**定理**:
"在子模性假设下,贪心选择为 (1-1/e)-近似"。这是论文理论故事从"启发式"到"有保证"的质变。

## 2. ILP 最优 oracle baseline(建议做,小工程)

$$\max \sum_{r} \hat g_r\, x_r \quad \text{s.t.} \sum_{r} \hat c_r\, x_r \le B,\ x_r \in \{0,1\}$$

region 数百、单条 knapsack 约束,ILP 单次求解秒级(pulp/Gurobi)。**注意:只需 ILP,不需要网络流。**

用途:解出"给定预测增益下贪心离最优差多少",画进主图当上界(第 8 条曲线或独立表):

- 贪心 vs ILP 差距小 → 免费获得"贪心已足够好"的实证,堵住"你的选择算法是不是太弱"的质疑;
- 差距大 → 说明选择算法本身有提升空间,届时再认真考虑更强的组合工具。

## 3. flow-routing 路由上限分析(可选,流在本课题中最自然的应用)

### 3.1 现状

路由 = "Base top-$k_{\text{route}}$ → 查表",确定性、零额外成本,但受限于 Base 的检索质量
(ARR/CRR 低时区域图再好也救不回 query)。

### 3.2 流式上界(离线,不改在线系统)

构造 query-文档二分图:query → 候选文档(容量 1),文档 → region(容量 $\infty$);每个 query 的
流出量 $\le k_{\text{route}}$;在总流量约束下最大化"触达 gold region"的权重。

一次最大流给出**路由阶段的理论最优 ARR/CRR**,量化"确定性路由离最优还有多远",
把 RQ4 的讨论从定性("存在天花板")升级为定量("离天花板还差 X")。

## 4. 执行策略

1. **先跑 1.0 实验,让数据定武器**:probe 增益分布与交互项分布出来之前,只做 §1 的证明框架准备;
2. **§2 ILP oracle 加进主图**——零风险、小工程、直接强化主结果的可信度;
3. **§3 路由上限**作为 RQ4 的量化分析小节(可选,离线计算);
4. 若交互数据证实**强正互补**(而非边际递减),再回头考虑依赖建模——但那是一个新问题定义,
   不属于当前论文范围。

一句话:**胜负手在统计/ML 侧(预测准不准),组合优化侧用"子模性声明 + ILP oracle"垫理论地基即可;
流只做路由上限的离线分析,不再承担选择算法的角色。**
